<h1>Listing curriculum_areas</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @curriculum_areas.each do |curriculum_area| %>
  <tr>
    <td><%= link_to 'Show', curriculum_area %></td>
    <td><%= link_to 'Edit', edit_curriculum_area_path(curriculum_area) %></td>
    <td><%= link_to 'Destroy', curriculum_area, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Curriculum area', new_curriculum_area_path %>
