<%= form_for [:admin, @challenge_type] do |f| %>
  <% if @challenge_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@challenge_type.errors.count, "error") %> prohibited this challenge_type from being saved:</h2>

      <ul>
      <% @challenge_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
