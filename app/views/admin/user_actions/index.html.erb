<h1>Listing actions</h1>

<table>
  <tr>
    <th>Action type</th>
    <th>Reward</th>
    <th>Risk</th>
    <th>Challenge</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_actions.each do |user_action| %>
  <tr>
    <td><%= user_action.action_type_id %></td>
    <td><%= user_action.reward_id %></td>
    <td><%= user_action.risk_id %></td>
    <td><%= user_action.challenge_id %></td>
    <td><%= link_to 'Show', [:admin, user_action] %></td>
    <td><%= link_to 'Edit', edit_admin_user_action_path(user_action) %></td>
    <td><%= link_to 'Destroy', [:admin, user_action], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Action', new_admin_user_action_path %>
