%h1 Listing items

%table
  %tr
    %th Item type
    %th Value
    %th
    %th
    %th

  - @items.each do |item|
    %tr
      %td= item.item_type_id
      %td= item.value
      %td= link_to 'Show', [:admin, item]
      %td= link_to 'Edit', edit_admin_item_path(item)
      %td= link_to 'Destroy', [:admin, item], confirm: 'Are you sure?', method: :delete

%br

= link_to 'New Item', new_admin_item_path
