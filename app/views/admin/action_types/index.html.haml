<h1>Listing action_types</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @action_types.each do |action_type| %>
  <tr>
    <td><%= link_to 'Show', [:admin, action_type] %></td>
    <td><%= link_to 'Edit', edit_admin_action_type_path(action_type) %></td>
    <td><%= link_to 'Destroy', [:admin, action_type], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Action type', new_admin_action_type_path %>
